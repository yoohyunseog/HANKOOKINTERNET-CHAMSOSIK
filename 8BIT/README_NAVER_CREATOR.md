# 네이버 크리에이터 어드바이저 트렌드 분석기

네이버 크리에이터 어드바이저의 트렌드 페이지에서 "설정순 보기" 데이터를 자동으로 수집하고 분석하는 프로그램입니다.

## 📋 주요 기능

- ✅ 네이버 자동 로그인
- ✅ 크리에이터 어드바이저 트렌드 페이지 접근
- ✅ "설정순 보기" 탭 데이터 자동 추출
- ✅ 키워드, 메트릭, 통계 데이터 수집
- ✅ JSON/CSV 형식으로 저장
- ✅ 예약 실행 (스케줄링)
- ✅ HTML 리포트 자동 생성
- ✅ 시계열 데이터 추적

## 🚀 빠른 시작

### 1. 설정 파일 수정

`config/naver_creator_config.json` 파일을 열어서 네이버 계정 정보를 입력하세요:

```json
{
  "naver_id": "your_naver_id",
  "naver_password": "your_password",
  "blog_id": "dbghwns2"
}
```

### 2. 프로그램 실행

**방법 1: 배치 파일 사용 (권장)**
```batch
run_naver_creator_analyzer.bat
```

**방법 2: Python 직접 실행**
```bash
# 패키지 설치
pip install -r 8BIT/requirements_naver_creator.txt

# 프로그램 실행
python 8BIT/naver_creator_trend_analyzer.py
```

### 3. 실행 모드 선택

프로그램 실행 시 두 가지 모드를 선택할 수 있습니다:

1. **즉시 실행 (1회)**: 지금 당장 한 번만 데이터를 수집
2. **예약 실행 (반복)**: 설정한 시간 간격마다 자동으로 데이터 수집

```
실행 모드를 선택하세요:
1. 즉시 실행 (1회)
2. 예약 실행 (반복)
선택 (1 or 2): 2

실행 간격(분) [기본값: 60]: 30
```

## 📊 데이터 확인

### 리포트 뷰어 실행

수집된 데이터를 보기 좋게 표시하려면:

```bash
python 8BIT/naver_creator_viewer.py
```

브라우저에서 HTML 리포트가 자동으로 열립니다.

### 저장된 파일 위치

모든 데이터는 `data/naver_creator_trends/` 폴더에 저장됩니다:

```
data/naver_creator_trends/
├── trend_data_20260218_143022.json  # 타임스탬프별 데이터
├── trend_data_20260218_143022.csv   # CSV 형식
├── latest_trend_data.json           # 최신 데이터 (항상 덮어쓰기)
├── screenshot_20260218_143022.png   # 페이지 스크린샷
└── report.html                      # HTML 리포트
```

## 📁 파일 구조

```
8BIT/
├── naver_creator_trend_analyzer.py  # 메인 프로그램
├── naver_creator_viewer.py          # 데이터 뷰어
└── requirements_naver_creator.txt   # 필요한 패키지

config/
└── naver_creator_config.json        # 설정 파일

data/naver_creator_trends/           # 데이터 저장 폴더
```

## ⚙️ 설정 옵션

`config/naver_creator_config.json` 파일의 전체 설정:

```json
{
  "naver_id": "your_naver_id",
  "naver_password": "your_password",
  "blog_id": "dbghwns2",
  "schedule": {
    "enabled": true,
    "interval_minutes": 60,
    "start_time": "09:00",
    "end_time": "22:00"
  },
  "output": {
    "save_json": true,
    "save_csv": true,
    "save_screenshot": true
  },
  "chrome_driver": {
    "headless": true,
    "window_size": "1920,1080"
  }
}
```

### 설정 항목 설명

| 항목 | 설명 | 기본값 |
|------|------|--------|
| `naver_id` | 네이버 로그인 ID | 필수 |
| `naver_password` | 네이버 비밀번호 | 필수 |
| `blog_id` | 크리에이터 블로그 ID | dbghwns2 |
| `interval_minutes` | 실행 간격 (분) | 60 |
| `headless` | 백그라운드 실행 여부 | true |

## 🔍 수집되는 데이터

### 트렌드 데이터
- 탭 제목
- 키워드 목록
- 메트릭 (조회수, 검색량 등)
- 타임스탬프

### 상세 데이터
- 테이블 데이터
- 리스트 데이터
- 차트 데이터

### 예시
```json
{
  "collection_time": "2026-02-18T14:30:22",
  "blog_id": "dbghwns2",
  "total_items": 15,
  "trend_data": [
    {
      "index": 1,
      "title": "트렌드 주제",
      "keywords": ["키워드1", "키워드2"],
      "metrics": {
        "조회수": "12345",
        "검색량": "6789"
      },
      "timestamp": "2026-02-18T14:30:22"
    }
  ]
}
```

## 🛠️ 문제 해결

### Chrome Driver 오류
```
selenium.common.exceptions.WebDriverException
```
**해결 방법**: Chrome 브라우저를 최신 버전으로 업데이트하세요.

### 로그인 실패
```
❌ 로그인 실패
```
**해결 방법**:
1. 네이버 ID/비밀번호를 다시 확인하세요.
2. 네이버 보안 설정에서 로봇이 아님을 확인해야 할 수 있습니다.
3. 2단계 인증이 활성화되어 있다면 해제하거나 앱 비밀번호를 사용하세요.

### 데이터 추출 실패
```
⚠️ 탭 추출 실패
```
**해결 방법**:
1. `headless: false`로 설정하여 브라우저 창을 보면서 디버깅하세요.
2. 스크린샷을 확인하여 페이지가 제대로 로드되었는지 확인하세요.
3. 페이지 구조가 변경되었을 수 있으므로 CSS 선택자를 업데이트해야 할 수 있습니다.

## 📝 사용 예시

### 예시 1: 매일 오전 9시에 데이터 수집

Windows 작업 스케줄러를 사용:
```batch
# 작업 이름: NaverCreatorTrendCollector
# 트리거: 매일 오전 9시
# 작업: run_naver_creator_analyzer.bat
```

### 예시 2: 30분마다 자동 수집

프로그램 실행 후:
```
실행 모드를 선택하세요:
1. 즉시 실행 (1회)
2. 예약 실행 (반복)
선택 (1 or 2): 2

실행 간격(분) [기본값: 60]: 30
```

### 예시 3: 특정 블로그 ID 분석

```python
from naver_creator_trend_analyzer import NaverCreatorTrendAnalyzer

analyzer = NaverCreatorTrendAnalyzer('your_id', 'your_password')
analyzer.analyze_and_collect(blog_id='your_blog_id')
```

## 🔒 보안 주의사항

- ⚠️ `config/naver_creator_config.json` 파일에는 비밀번호가 평문으로 저장됩니다.
- ⚠️ 이 파일을 Git에 커밋하지 마세요! (`.gitignore`에 추가 권장)
- ⚠️ 다른 사람과 공유하지 마세요.

## 📈 고급 활용

### 데이터 분석 예시

```python
import pandas as pd
import json
from pathlib import Path

# 모든 데이터 로드
data_files = sorted(Path('data/naver_creator_trends').glob('trend_data_*.json'))
all_data = []

for file in data_files:
    with open(file, 'r', encoding='utf-8') as f:
        data = json.load(f)
        all_data.append(data)

# DataFrame으로 변환
df = pd.DataFrame(all_data)

# 시간대별 트렌드 분석
df['collection_time'] = pd.to_datetime(df['collection_time'])
df.groupby(df['collection_time'].dt.hour)['total_items'].mean()
```

## 🤝 기여

버그 리포트나 기능 제안은 Issues에 등록해주세요.

## 📄 라이선스

MIT License

## ⚡ 업데이트 내역

- **v1.0.0** (2026-02-18)
  - 초기 릴리스
  - 기본 데이터 수집 기능
  - 예약 실행 기능
  - HTML 리포트 생성

---

Made with ❤️ for Naver Creators
